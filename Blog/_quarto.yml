project:
  type: website

website:
  description: "A blog built with Quarto"
  title: "Blog"

# Temporarily commented out iconify filter due to rendering issues
# filters:
#   - iconify

format:
  html:
    theme:
      - lux
      - brand
    css: styles.css

execute:
  freeze: false

